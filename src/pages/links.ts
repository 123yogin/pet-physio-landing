/** Thin re-exports so page templates import link helpers from one place. */
export { Link, useRouter } from '../seo/router';
export { conditionPath, servicePath as servicePathLink, specialistPath } from '../seo/routes';
